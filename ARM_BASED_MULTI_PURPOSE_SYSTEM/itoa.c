#include <stdio.h>
#include <string.h>

int main(void) {
    int input;
	int i, length, base;
    printf("Enter the integer string: ");
    scanf("%d",&input);
    printf("Enter the base: ");
    scanf("%d",&base);
    
    
    //printf("ASCII value is: %c",input);
    
    /*length = strlen(input);
    for(i=0;i<length;i++)
    {
    	printf("%d",input[i]);
	}*/
	return 0;
}

